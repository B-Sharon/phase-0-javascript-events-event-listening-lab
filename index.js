

function addingEventListener() {
    //grabbing the element that we want to listen to
    const input = document.getElementById('button');

    //adding an event listener
    input.addEventListener('click', () => {
        alert("I was clicked!")} )
    }
   
addingEventListener();


